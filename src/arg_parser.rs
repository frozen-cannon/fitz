pub mod arg_parser {
    pub fn parse(mut args: Vec<String>) {
        // d
    }
}

#[cfg(test)]
mod tests {
    use crate::arg_parser::arg_parser::parse;

    use super::*;

    #[test]
    fn test_args() {
        let args = vec!["--list"];
        // let
        // assert_eq!(parse(args),)
    }
}
